name: ${name}
version: ${version}

mains:
  papermc: "${group}.papermc.Template"
  spigotmc: "${group}.spigotmc.Template"
  foliamc: "${group}.foliamc.Template"

git:
  provider: github
  owner: MCPaimon
  repository: extension-template
